<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Minimal Theme - Clean white/black, no decorations -->
    
    <!-- Primary Colors -->
    <Color x:Key="PrimaryColor">#171717</Color>
    <Color x:Key="PrimaryHoverColor">#262626</Color>
    <Color x:Key="PrimaryPressedColor">#0A0A0A</Color>
    
    <!-- Background Colors -->
    <Color x:Key="BackgroundColor">#FAFAFA</Color>
    <Color x:Key="CardBackgroundColor">#FFFFFF</Color>
    <Color x:Key="OverlayBackgroundColor">#FFFFFF</Color>
    <Color x:Key="OverlayInfoBackgroundColor">#FAFAFA</Color>
    
    <!-- Text Colors -->
    <Color x:Key="ForegroundColor">#171717</Color>
    <Color x:Key="SecondaryTextColor">#737373</Color>
    
    <!-- Border Colors -->
    <Color x:Key="BorderColor">#E5E5E5</Color>
    <Color x:Key="OverlayBorderColor">#D4D4D4</Color>
    
    <!-- Additional Colors -->
    <Color x:Key="ProgressBackgroundColor">#E5E5E5</Color>
    <Color x:Key="AlbumArtPlaceholderColor">#F5F5F5</Color>
    <Color x:Key="SourceBadgeBackgroundColor">#171717</Color>
    <Color x:Key="SourceBadgeForegroundColor">#FFFFFF</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
    <SolidColorBrush x:Key="PrimaryHoverBrush" Color="{StaticResource PrimaryHoverColor}" />
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource CardBackgroundColor}" />
    <SolidColorBrush x:Key="OverlayBackgroundBrush" Color="{StaticResource OverlayBackgroundColor}" />
    <SolidColorBrush x:Key="OverlayInfoBackgroundBrush" Color="{StaticResource OverlayInfoBackgroundColor}" />
    <SolidColorBrush x:Key="ForegroundBrush" Color="{StaticResource ForegroundColor}" />
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="{StaticResource SecondaryTextColor}" />
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
    <SolidColorBrush x:Key="OverlayBorderBrush" Color="{StaticResource OverlayBorderColor}" />
    <SolidColorBrush x:Key="ProgressBackgroundBrush" Color="{StaticResource ProgressBackgroundColor}" />
    <SolidColorBrush x:Key="AlbumArtPlaceholderBrush" Color="{StaticResource AlbumArtPlaceholderColor}" />
    <SolidColorBrush x:Key="SourceBadgeBackgroundBrush" Color="{StaticResource SourceBadgeBackgroundColor}" />
    <SolidColorBrush x:Key="SourceBadgeForegroundBrush" Color="{StaticResource SourceBadgeForegroundColor}" />

    <!-- Converters -->
    <BooleanToVisibilityConverter x:Key="BoolToVisibility" />

    <!-- Button Styles - Minimal -->
    <Style x:Key="OverlayButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="#171717" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            CornerRadius="25"
                            Padding="5">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#10000000" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#20000000" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TabItem Style -->
    <Style TargetType="TabItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Border" Padding="12,8" Margin="0,0,5,0" CornerRadius="6">
                        <ContentPresenter ContentSource="Header" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryBrush}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="False">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource CardBackgroundBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
    </Style>

</ResourceDictionary>
