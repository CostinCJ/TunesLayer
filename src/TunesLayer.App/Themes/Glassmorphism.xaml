<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Glassmorphism Theme - Frosted glass with blur effect -->
    
    <!-- Primary Colors -->
    <Color x:Key="PrimaryColor">#60A5FA</Color>
    <Color x:Key="PrimaryHoverColor">#93C5FD</Color>
    <Color x:Key="PrimaryPressedColor">#3B82F6</Color>
    
    <!-- Background Colors (Semi-transparent for glass effect) -->
    <Color x:Key="BackgroundColor">#E8F0FE</Color>
    <Color x:Key="CardBackgroundColor">#B0FFFFFF</Color>
    <Color x:Key="OverlayBackgroundColor">#90FFFFFF</Color>
    <Color x:Key="OverlayInfoBackgroundColor">#A0FFFFFF</Color>
    
    <!-- Text Colors -->
    <Color x:Key="ForegroundColor">#1E3A5F</Color>
    <Color x:Key="SecondaryTextColor">#64748B</Color>
    
    <!-- Border Colors -->
    <Color x:Key="BorderColor">#40FFFFFF</Color>
    <Color x:Key="OverlayBorderColor">#60FFFFFF</Color>
    
    <!-- Additional Colors -->
    <Color x:Key="ProgressBackgroundColor">#30000000</Color>
    <Color x:Key="AlbumArtPlaceholderColor">#20000000</Color>
    <Color x:Key="SourceBadgeBackgroundColor">#60A5FA</Color>
    <Color x:Key="SourceBadgeForegroundColor">#FFFFFF</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
    <SolidColorBrush x:Key="PrimaryHoverBrush" Color="{StaticResource PrimaryHoverColor}" />
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource CardBackgroundColor}" />
    <SolidColorBrush x:Key="OverlayBackgroundBrush" Color="{StaticResource OverlayBackgroundColor}" />
    <SolidColorBrush x:Key="OverlayInfoBackgroundBrush" Color="{StaticResource OverlayInfoBackgroundColor}" />
    <SolidColorBrush x:Key="ForegroundBrush" Color="{StaticResource ForegroundColor}" />
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="{StaticResource SecondaryTextColor}" />
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
    <SolidColorBrush x:Key="OverlayBorderBrush" Color="{StaticResource OverlayBorderColor}" />
    <SolidColorBrush x:Key="ProgressBackgroundBrush" Color="{StaticResource ProgressBackgroundColor}" />
    <SolidColorBrush x:Key="AlbumArtPlaceholderBrush" Color="{StaticResource AlbumArtPlaceholderColor}" />
    <SolidColorBrush x:Key="SourceBadgeBackgroundBrush" Color="{StaticResource SourceBadgeBackgroundColor}" />
    <SolidColorBrush x:Key="SourceBadgeForegroundBrush" Color="{StaticResource SourceBadgeForegroundColor}" />

    <!-- Converters -->
    <BooleanToVisibilityConverter x:Key="BoolToVisibility" />

    <!-- Button Styles with Glass Effect -->
    <Style x:Key="OverlayButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#40FFFFFF" />
        <Setter Property="Foreground" Value="#1E3A5F" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="#60FFFFFF" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="25"
                            Padding="5">
                        <Border.Effect>
                            <DropShadowEffect Color="#60A5FA" BlurRadius="10" ShadowDepth="0" Opacity="0" />
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#60FFFFFF" />
                            <Setter TargetName="border" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="#60A5FA" BlurRadius="15" ShadowDepth="0" Opacity="0.5" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#80FFFFFF" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TabItem Style -->
    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Border" Padding="12,8" Margin="2,0" Background="Transparent"
                            BorderThickness="0,0,0,2" BorderBrush="Transparent">
                        <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource CardBackgroundBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
