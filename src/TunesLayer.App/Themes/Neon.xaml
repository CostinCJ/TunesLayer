<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Neon Theme - Cyberpunk aesthetic with glow effects -->
    
    <!-- Primary Colors -->
    <Color x:Key="PrimaryColor">#00FF88</Color>
    <Color x:Key="PrimaryHoverColor">#33FFAA</Color>
    <Color x:Key="PrimaryPressedColor">#00CC6A</Color>
    
    <!-- Background Colors -->
    <Color x:Key="BackgroundColor">#0A0A0F</Color>
    <Color x:Key="CardBackgroundColor">#0F1118</Color>
    <Color x:Key="OverlayBackgroundColor">#0D0D14</Color>
    <Color x:Key="OverlayInfoBackgroundColor">#080810</Color>
    
    <!-- Text Colors -->
    <Color x:Key="ForegroundColor">#E0FFE0</Color>
    <Color x:Key="SecondaryTextColor">#66FF99</Color>
    
    <!-- Border Colors -->
    <Color x:Key="BorderColor">#00FF8840</Color>
    <Color x:Key="OverlayBorderColor">#00FF8860</Color>
    
    <!-- Additional Colors -->
    <Color x:Key="ProgressBackgroundColor">#1A2A1A</Color>
    <Color x:Key="AlbumArtPlaceholderColor">#0F1A0F</Color>
    <Color x:Key="SourceBadgeBackgroundColor">#00FF8820</Color>
    <Color x:Key="SourceBadgeForegroundColor">#00FF88</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
    <SolidColorBrush x:Key="PrimaryHoverBrush" Color="{StaticResource PrimaryHoverColor}" />
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource CardBackgroundColor}" />
    <SolidColorBrush x:Key="OverlayBackgroundBrush" Color="{StaticResource OverlayBackgroundColor}" />
    <SolidColorBrush x:Key="OverlayInfoBackgroundBrush" Color="{StaticResource OverlayInfoBackgroundColor}" />
    <SolidColorBrush x:Key="ForegroundBrush" Color="{StaticResource ForegroundColor}" />
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="{StaticResource SecondaryTextColor}" />
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
    <SolidColorBrush x:Key="OverlayBorderBrush" Color="{StaticResource OverlayBorderColor}" />
    <SolidColorBrush x:Key="ProgressBackgroundBrush" Color="{StaticResource ProgressBackgroundColor}" />
    <SolidColorBrush x:Key="AlbumArtPlaceholderBrush" Color="{StaticResource AlbumArtPlaceholderColor}" />
    <SolidColorBrush x:Key="SourceBadgeBackgroundBrush" Color="{StaticResource SourceBadgeBackgroundColor}" />
    <SolidColorBrush x:Key="SourceBadgeForegroundBrush" Color="{StaticResource SourceBadgeForegroundColor}" />

    <!-- Converters -->
    <BooleanToVisibilityConverter x:Key="BoolToVisibility" />

    <!-- Button Styles with Neon Glow -->
    <Style x:Key="OverlayButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="#00FF88" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            CornerRadius="25"
                            Padding="5">
                        <Border.Effect>
                            <DropShadowEffect Color="#00FF88" BlurRadius="0" ShadowDepth="0" />
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#2000FF88" />
                            <Setter TargetName="border" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="#00FF88" BlurRadius="15" ShadowDepth="0" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#4000FF88" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TabItem Style -->
    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="#FFFFFF" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Border" Padding="12,8" Margin="2,0" Background="Transparent"
                            BorderThickness="0,0,0,2" BorderBrush="Transparent">
                        <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource CardBackgroundBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
