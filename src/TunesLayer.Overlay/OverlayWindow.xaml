<Window x:Class="TunesLayer.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:TunesLayer.Overlay.Converters"
        mc:Ignorable="d"
        Title="TunesLayer"
        Width="250" Height="250"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Loaded="Window_Loaded">
    
    <Window.Resources>
        <!-- Converters -->
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility" />
        
        <!-- Animations -->
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2" />
        </Storyboard>
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.2" />
        </Storyboard>
        <Storyboard x:Key="ControlsFadeIn">
            <DoubleAnimation Storyboard.TargetName="ControlsPanel" Storyboard.TargetProperty="Opacity" 
                             From="0" To="1" Duration="0:0:0.15" />
        </Storyboard>
        <Storyboard x:Key="ControlsFadeOut">
            <DoubleAnimation Storyboard.TargetName="ControlsPanel" Storyboard.TargetProperty="Opacity" 
                             From="1" To="0" Duration="0:0:0.15" />
        </Storyboard>
    </Window.Resources>
    
    <Grid>
        <!-- Main Container with rounded corners and shadow -->
        <Border x:Name="MainBorder"
                CornerRadius="16"
                Background="{DynamicResource OverlayBackgroundBrush}"
                BorderBrush="{DynamicResource OverlayBorderBrush}"
                BorderThickness="1"
                Margin="10">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="0" Opacity="0.5" />
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <!-- Album Art -->
                <Grid Grid.Row="0">
                    <!-- Placeholder when no album art -->
                    <Border Background="{DynamicResource AlbumArtPlaceholderBrush}"
                            CornerRadius="16,16,0,0"
                            Visibility="{Binding HasAlbumArt, Converter={StaticResource InverseBoolToVisibility}}">
                        <TextBlock Text="&#xE8D6;" 
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="48"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </Border>
                    
                    <!-- Album Art Image - using Border with ImageBrush for proper corner clipping -->
                    <Border x:Name="AlbumArtBorder" CornerRadius="16,16,0,0">
                        <Border.Background>
                            <ImageBrush ImageSource="{Binding AlbumArt}" 
                                        Stretch="UniformToFill"
                                        RenderOptions.BitmapScalingMode="HighQuality" />
                        </Border.Background>
                    </Border>
                    
                    <!-- Gradient overlay for text readability -->
                    <Border CornerRadius="16,16,0,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="Transparent" Offset="0.5" />
                                <GradientStop Color="#CC000000" Offset="1" />
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>
                    
                    <!-- Source App Indicator -->
                    <Border HorizontalAlignment="Right" 
                            VerticalAlignment="Top"
                            Background="{DynamicResource SourceBadgeBackgroundBrush}"
                            CornerRadius="0,15,0,8"
                            Padding="8,4">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding SourceAppIcon}" 
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="12"
                                       Foreground="{DynamicResource SourceBadgeForegroundBrush}"
                                       VerticalAlignment="Center" />
                            <TextBlock Text="{Binding SourceAppName}" 
                                       FontSize="10"
                                       Foreground="{DynamicResource SourceBadgeForegroundBrush}"
                                       VerticalAlignment="Center"
                                       Margin="4,0,0,0" />
                        </StackPanel>
                    </Border>
                    
                    <!-- Controls Overlay (appears on hover) -->
                    <Grid x:Name="ControlsPanel" Opacity="0"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Center">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button x:Name="PrevButton" 
                                    Click="PrevButton_Click"
                                    Style="{DynamicResource OverlayButtonStyle}"
                                    Width="40" Height="40" Margin="5">
                                <TextBlock Text="&#xE892;" FontFamily="Segoe MDL2 Assets" FontSize="16" />
                            </Button>
                            <Button x:Name="PlayPauseButton" 
                                    Click="PlayPauseButton_Click"
                                    Style="{DynamicResource OverlayButtonStyle}"
                                    Width="50" Height="50" Margin="5">
                                <TextBlock x:Name="PlayPauseIcon" Text="&#xE768;" FontFamily="Segoe MDL2 Assets" FontSize="20" />
                            </Button>
                            <Button x:Name="NextButton" 
                                    Click="NextButton_Click"
                                    Style="{DynamicResource OverlayButtonStyle}"
                                    Width="40" Height="40" Margin="5">
                                <TextBlock Text="&#xE893;" FontFamily="Segoe MDL2 Assets" FontSize="16" />
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
                
                <!-- Track Info -->
                <Border Grid.Row="1" Padding="12,6,12,8" CornerRadius="0,0,16,16" Background="{DynamicResource OverlayInfoBackgroundBrush}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        
                        <!-- Track Title -->
                        <TextBlock Grid.Row="0"
                                   x:Name="TrackTitleText"
                                   Text="{Binding TrackTitle}"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource ForegroundBrush}"
                                   TextTrimming="CharacterEllipsis"
                                   ToolTip="{Binding TrackTitle}" />
                        
                        <!-- Artist + Time Row -->
                        <Grid Grid.Row="1" Margin="0,2,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0"
                                       Text="{Binding ArtistName}"
                                       FontSize="11"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center" />
                            
                            <StackPanel Grid.Column="1" 
                                        Orientation="Horizontal" 
                                        VerticalAlignment="Center"
                                        Margin="8,0,0,0">
                                <TextBlock Text="{Binding CurrentTime}" 
                                           FontSize="11"
                                           Foreground="{DynamicResource SecondaryTextBrush}" />
                                <TextBlock Text=" / " 
                                           FontSize="11"
                                           Foreground="{DynamicResource SecondaryTextBrush}" />
                                <TextBlock Text="{Binding TotalTime}" 
                                           FontSize="11"
                                           Foreground="{DynamicResource SecondaryTextBrush}" />
                            </StackPanel>
                        </Grid>
                        
                        <!-- Progress Bar -->
                        <ProgressBar Grid.Row="2"
                                     Value="{Binding Progress}"
                                     Maximum="100"
                                     Height="3"
                                     Margin="0,4,0,0"
                                     Background="{DynamicResource ProgressBackgroundBrush}"
                                     Foreground="{DynamicResource PrimaryBrush}"
                                     BorderThickness="0" />
                    </Grid>
                </Border>
                </Grid>
        </Border>
    </Grid>
</Window>
